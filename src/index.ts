// ---------------------------------------------------------------------------
// Entry point
// ---------------------------------------------------------------------------

import { createAdapter } from "./utils/createAdapter";

function main(): void {
    createAdapter();
}

main();
